# خطة تحسين الأمن والأداء — الجولة الثانية

تاريخ الإعداد: 2026-09-05. مرجع الكود عند الفحص: `e59dcfa`.

الحالة: **قيد التنفيذ؛ اكتمل خط الأساس والدفعة الأولى من حماية الشبكة والطلبات، مع تحسين أول استعلامين متعددَي المصادر**.

## 1. الهدف والنطاق

تحسين حماية EIAMS وسرعته اعتمادًا على قياسات واختبارات قابلة للتكرار، مع المحافظة على صحة الأرصدة، تاريخ الحركات والتدقيق، صلاحيات المستخدم ونطاقه، وذرّية الترحيل والعكس.

تشمل الخطة ASP.NET Core وApplication وInfrastructure وPostgreSQL والتخزين والمصادقة والكاش والتقارير والمهام الخلفية وCI وإعدادات النشر. يبقى نموذج المستخدم: دور واحد ونطاق واحد مع الوصول الهرمي المسموح. تبقى مسارات `/api/v1` وشكل استجابات النجاح والخطأ الحاليان، إلا عند توثيق ترحيل تعاقدي محدد.

لا نعدل الفرونت. أي تغيير مطلوب منه، مثل طريقة تجديد الجلسة، يوثّق عند التنفيذ في `security-performance-v2/FRONTEND-HANDOFF.md` مع أمثلة request/response وخطوات الانتقال.

الخطة مبنية على قراءة الكود الحالي والخطط السابقة ومصادر رسمية، باستخدام `security-scan` و`postgresql-optimization` وممارسات .NET المناسبة لبنية المشروع. نحافظ على xUnit وShouldly وNSubstitute الموجودة؛ لا نبدل أدوات الاختبار لمجرد اختلاف مثال داخل skill.

هذه مراجعة تمهيدية للتخطيط، وليست اختبار اختراق أو إثباتًا لسرعة الإنتاج. نتائج الاختبارات المذكورة في التقرير السابق تاريخية؛ لم نعد تشغيلها لإعداد هذا الملف، ولم نفحص قاعدة إنتاج أو نغير أسرارًا.

## 2. ما نحافظ عليه وما نعيد تقييمه

الموجود بالفعل ويجب اختبار عدم تراجعه:

- JWT validation وhash كلمات المرور وrefresh tokens، وتصحيح cookie path.
- حماية آخر Administrator وثبات دور Administrator الأساسي.
- التحقق من signatures للمرفقات ومفاتيح التخزين.
- pagination وaudit cursor pagination، وbatch holder resolution.
- ledger يستخدم `unnest` لمطابقة أزواج المستودع والمادة بدقة.
- هجرة `AddTrigramSearchIndexes`، وHybridCache، وعداد SQL للاختبارات، وقياس handler duration.
- اختبارات المعمارية والتكامل، ومراجعة الحزم داخل CI.

الخطط القديمة ليست وصفًا دقيقًا لكل الكود الحالي. مثال: مسار bootstrap الحقيقي في `RegisterController` هو `POST /api/v1/admin/users/register`؛ مصفوفة التقرير السابق كتبته تحت `/auth/register`. نولد جرد المسارات من التطبيق بدل نسخ القائمة القديمة.

## 3. أدلة الجولة الجديدة وأولوياتها

الأولوية P0 تعني البدء بالإثبات والمعالجة قبل تحسينات السرعة؛ لا تعني تلقائيًا أن الخطورة Critical.

| ID | الأولوية | الدليل الحالي | الأثر أو الفرضية التي سنختبرها |
|---|---|---|---|
| SP-01 | P0 | `ForwardedHeadersExtensions` يفرغ `KnownProxies` و`KnownIPNetworks` ثم لا يضيف شيئًا عندما يغيب الإعداد | كود ASP.NET Core 10.0.7 يجعل التحقق من IP مشروطًا بوجود عناصر في إحدى القائمتين؛ قد يصبح مصدر الطلب وscheme قابلين للتزوير عند وصول العميل مباشرة للتطبيق. هذا يناقض تعليق الكود والتقرير السابق |
| SP-02 | P0 | `ApplicationDbContext.SaveChangesAsync` يبطل الكاش قبل `CommitAsync` في المعاملة الخارجية | احتمال أن يعيد طلب آخر تعبئة الكاش من البيانات القديمة قبل commit فتستمر صلاحية ملغاة؛ يحتاج اختبار توقيت حتمي |
| SP-03 | P0 | Refresh يقفل hash الرمز؛ Logout بلا القفل نفسه؛ Suspend يستخدم قفل Administrator مختلفًا | سباق قد يبقي رمزًا خلفًا صالحًا بعد Logout أو يتعارض مع الإبطال؛ نختبر الرمز القديم والجديد معًا |
| SP-04 | P0 | `GetCustodiesQueryHandler` و`CounterpartResolver.SearchActiveAsync` يستخدمان `fetchLimit = offset + pageSize` وسعة قائمة مضروبة في 3 أو 4 | الصفحات العميقة ترفع القراءة والذاكرة، وقد تتجاوز ضربات السعة حدود الأعداد؛ الحد المركزي للصفحة يحمي جزءًا من الحساب وليس الكلفة |
| SP-05 | P1 | `UseAuthorization` يسبق `UseRateLimiter` | الطلب المرفوض بصلاحياته قد ينفذ عملًا مكلفًا دون بلوغ limiter؛ نحتاج حماية قبل العمل المكلف مع حفظ دلالات 401/403/429 |
| SP-06 | P1 | `AddHybridCache()` محلي، ومنح المستخدم مخبأة 10 دقائق | الإبطال في نسخة واحدة لا يثبت صحة نسخة أخرى؛ يحتاج اختبار نسختين قبل التوسع |
| SP-07 | P1 | `PasswordHasher`: PBKDF2-SHA512 بـ500000 دورة وصيغة hash لا تحمل الإصدار والكلفة | كلفة CPU محتملة في Login؛ تغيير العدد مباشرة يكسر التحقق من hashes القديمة |
| SP-08 | P1 | Benchmark يستخدم 7 warm iterations و3 تكرارات login ويجمع failures ويطبعها | لا يكفي للحكم على p95/p99 أو الحمل، وبعض فشل endpoints لا يجعل الاختبار يفشل صراحة |
| SP-09 | P1 | bootstrap يسمح بالتسجيل المجهول حين لا يوجد أي مستخدم | أول طلب لشبكة عامة قد يستولي على تثبيت جديد؛ القفل يمنع الازدواج ولا يثبت أن صاحب الطلب هو المشغل |
| SP-10 | P1 | `SecurityConfigurationExtensions` يرفض قيمة `*` كاملة فقط | نختبر قوائم hosts مثل `api.example.com;*` والقيم المشوهة، وبيئة Performance التي تمر بمسار الإنتاج |
| SP-11 | P1 | JSON يحمل refresh token مع HttpOnly cookie، والـbody له الأولوية | تعرض الرمز لJavaScript وتعارض مصادر الرمز يحتاجان قرار عقد جلسة واضحًا وتنسيقًا مع الفرونت |
| SP-12 | P2 | CI يعمل عند push إلى main أو يدويًا، وActions على tags، وفحص CVEs يحلل عبارة نصية | نحتاج فحص PRs وتثبيت مصدر الاعتماديات وفشل صريح عند تعذر الفحص |

SP-01 مؤكد من قراءة إعدادنا وكود framework؛ قابلية استغلاله تعتمد على topology وإصدار التشغيل الفعلي. SP-02 وSP-03 سيناريوهات تزامن مدعومة بالكود لكنها لم تُثبت بتنفيذ جديد. لا نسميهما ثغرات مثبتة قبل الاختبار.

## 4. ترتيب التنفيذ واعتمادياته

`0 Baseline → 1 حماية الدخول والموارد → 2 الجلسات → 3 الكاش والصلاحيات → 4 قياس الأداء → 5 Login → 6 القراءة والبحث → 7 الكتابة والتدقيق → 8 قاعدة البيانات → 9 الملفات → 10 CI والمراقبة → 11 التحقق والتسليم`.

كل مرحلة تنتج diff محدودًا واختبارات مرتبطة ودليل قبل/بعد. لا نعتبرها مكتملة بمرور build وحده. أي تغيير يمس صلاحيات أو معاملات يتطلب PostgreSQL integration test. يمكن جمع أدلة المراحل المتأخرة مبكرًا، لكن لا نؤجل معالجة P0 المثبتة انتظارًا لاختبار حمل طويل.

## المرحلة 0 — تثبيت الوضع والجرد

- [x] تسجيل commit وSDK/runtime وNpgsql/PostgreSQL وإعدادات التشغيل دون أسرار، ومراجعة worktree.
- [x] تشغيل build واختبارات الوحدة والمعمارية والتكامل وتسجيل skipped وأسبابها.
- [x] إعادة تدقيق NuGet المباشر والمتعدي والأسرار الحالية والتاريخية مع تنقيح النتائج؛ لا نفترض أن نتيجة 2026-09-03 ما زالت صالحة.
- [x] استخراج endpoint matrix من metadata الفعلي: method/path، anonymous، permission، نطاق البيانات، limiter، body limit، handler، والاختبار الذي يغطيه.
- [x] تصنيف كل بند قديم: موجود، ناقص، متراجع، أو تشغيلي غير متحقق. تصحيح assertions القديمة غير المدعومة، خصوصًا الثقة بالبروكسي.
- [ ] إنشاء بيئة اختبارات منفصلة؛ لا نعتمد بيانات المستخدم لاختبار الأمان أو الحمل.

المخرجات: `security-performance-v2/BASELINE.md` و`ENDPOINT-MATRIX.md` و`FINDINGS.md`.

بوابة الانتهاء: جرد مكتمل وحدود المراجعة معروفة، ولا فشل قديم غير موثق. استعادة بيئة الاختبار فقط عند الحاجة، دون تغيير بيانات التشغيل.

## المرحلة 1 — الثقة بالشبكة وحدود استهلاك الموارد

الملفات الأساسية: `Program.cs`، `ForwardedHeadersExtensions`، `SecurityConfigurationExtensions`، `RateLimitingExtensions`، إعدادات الطلبات وpagination.

- [x] تعطيل معالجة forwarded headers عند عدم إعداد proxy موثوق، أو استخدام إعداد مغلق مكافئ مثبت بالاختبار. لا نستخدم قوائم فارغة بمعنى «لا تثق بأحد».
- [x] ضبط trusted peers وhop limit بما يطابق النشر؛ رفض شبكات الثقة الشاملة غير المقصودة.
- [x] تحليل قائمة AllowedHosts عنصرًا عنصرًا واختبار wildcard المضمن والفراغات والقيم غير الصالحة. ضبط Performance صراحة دون تعطيل حمايات الإنتاج لقياس سرعة أفضل.
- [x] طبقة IP/مستخدم قبل التفويض المكلف، مع إثبات أن الطلب الثاني المرفوض يصل إلى 429 قبل إعادة عمل التفويض. المحدد العام الحالي محلي لكل نسخة.
- [ ] حدود concurrency لLogin والرفع والتقارير وposting، مع queue محدودة أو صفر؛ فصل rate عن concurrency، ومراعاة مستخدمين خلف NAT واحد.
- [x] حدود body لمسارات المصادقة، وحدود صريحة للبريد وكلمة المرور وrefresh token، مع رفض المدخلات غير المحدودة قبل PBKDF2.
- [ ] استكمال حدود البحث ومجموعات IDs والصفحات لبقية المسارات، واختبار الحسابات دون overflow.
- [ ] عند التشغيل متعدد النسخ: limiter على gateway أو آلية موزعة مثبتة؛ المحدد المحلي ليس حدًا عالميًا.

اختبارات الإثبات: peer معروف/مجهول مع X-Forwarded-For وProto مزورين؛ طلبات مرفوضة بالصلاحيات تحت الحمل؛ الصفحة القصوى؛ body ضخم؛ 429 مع envelope وRetry-After؛ health probes لا تسبب فقد الجاهزية بسبب limiter عام.

بوابة الانتهاء: لا يغير peer غير موثوق الهوية الشبكية أو scheme، والطلب غير المقبول لا يستهلك تخصيصًا غير محدود أو SQL متكررًا. التراجع يكون إلى نشر مغلق أو نسخة آمنة، لا إلى الثقة بجميع البروكسيات.

## المرحلة 2 — دورة الجلسة وتهيئة أول مدير

- [ ] تصميم تسلسل واحد لRefresh وLogout وإبطال الجلسات وتعليق المستخدم: قفل على session/family أو user وفق نموذج واضح، مع ترتيب ثابت للأقفال وإعادة قراءة الحالة بعد القفل.
- [ ] اختبار `old refresh ↔ new refresh` و`refresh ↔ logout` و`refresh ↔ suspend` وlogin أثناء التعليق، دون sleep عشوائي؛ استعمال barriers للتحكم بالتوقيت.
- [ ] توثيق أن replay الحالي يبطل جميع refresh tokens للمستخدم، وليس family ممثلة صراحة في schema. اختيار إبقاء سياسة كل الجلسات أو إضافة SessionId/FamilyId بهجرة واضحة؛ لا تغيير صامت.
- [ ] معالجة الإبطال الذي يجب حفظه رغم نتيجة المصادقة الفاشلة كـoutcome صريح؛ المحافظة على rollback لأخطاء العمل العادية.
- [ ] قرار access-token revocation: Logout الحالي لا يثبت إبطال access token فورًا. نحدد هل المطلوب session version/check أو انتهاء قصير، ونقيس كلفة الخيار ونوثقه.
- [ ] bootstrap مغلق افتراضيًا في الإنتاج: provisioning تشغيلي محدود/CLI، أو تفعيل مؤقت بسر مستقل أحادي الاستخدام. حساب فارغ وحده لا يكفي لإجازة أول مدير.
- [ ] فصل recovery لمدير مفقود عن التسجيل العام، مع audit وسر مؤقت وانتهاء وإبطال جلسات؛ لا حذف audit لاستعادة الدخول.
- [ ] انتقال browser refresh إلى HttpOnly/Secure cookie وفق عقد متدرج؛ CSRF/Origin checks حيث تستخدم cookies، واختبار same-site وcross-site حسب نطاقات النشر الفعلية.
- [ ] عدم إصدار refresh token في JSON بعد انتقال الفرونت؛ إن احتاج عميل غير متصفح body-token فله سياسة واضحة. رفض أو تحديد تعارض cookie/body.
- [ ] تثبيت algorithms المسموحة في JWT وخطة دوران مفاتيح مع تداخل محدود، والتحقق من expiry boundary وGUID فارغ وحالة المستخدم وتسجيل الأحداث دون رموز.

بوابة الانتهاء: بعد اكتمال الإبطال لا يُنتج سباق رمزًا قابلًا للاستخدام مخالفًا للعقد. أول مدير لا يستطيع إنشاءه عميل عام غير مصرح. اختبارات العميل القديم والجديد تنجح أثناء نافذة الانتقال.

التراجع: migrations إضافية متوافقة أولًا؛ لا نحذف بيانات session القديمة قبل اكتمال الانتقال، ولا نعيد تمكين bootstrap العام للتراجع.

## المرحلة 3 — اتساق الصلاحيات والكاش مع المعاملات

- [ ] إعادة إنتاج سيناريو: SaveChanges → invalidation → قراءة متزامنة للبيانات القديمة → commit → طلب محمي.
- [ ] تأجيل invalidation المرتبط بمعاملة حتى نجاح commit، مع مسار صحيح للحفظ بلا معاملة صريحة. rollback لا ينشر تغييرات وهمية.
- [ ] تغطية فشل العملية بعد commit وقبل invalidation: durable outbox/retry أو authorization version وفحص موثوق، وفق تكلفة وتعقيد موثقين.
- [ ] معالجة إعادة تعبئة cache بدأت قبل الإبطال واكتملت بعده؛ post-commit وحده ليس برهانًا كافيًا على إزالة هذا السباق.
- [ ] منع stale authorization للمستخدم المعلق أو صاحب الدور المسحوب وفق نافذة إبطال معلنة؛ العمليات الحساسة تعتمد حالة/version موثوقة عند التنفيذ.
- [ ] اختبار تطبيقين مستقلين على DB واحدة. Redis وحده لا يُعد دليلًا على تزامن L1؛ نختار backplane/versioning أو سياسة اتساق مناسبة ونختبر تعطل القناة.
- [ ] تضييق tags الواسعة عند إثبات فائدته، وإضافة hit/miss وinvalidation lag وstampede metrics؛ مفاتيح cache تشمل هوية/نطاق/فلاتر الطلب عند الحاجة.
- [ ] مراجعة domain events داخل SaveChanges: فصل الأحداث اللازمة لصحة المعاملة عن side effects التي يجب نشرها بعد commit.

بوابة الانتهاء: tests حتمية لإبطال grants/suspend/rollback ونسختين وتوقف قناة الإبطال. صحة الصلاحية أعلى من hit ratio. نسمح fallback لقراءة DB ضمن حد الحمل عند تعطل الكاش.

## المرحلة 4 — قياس أداء ممثل وقابل للإعادة

- [ ] تطوير benchmark الموجود بدل استبداله كله؛ أي endpoint فاشل يجعل النتيجة فشلًا، وتُحفظ نتائج باسم run فريد مع commit/environment/seed.
- [ ] قياس HTTP حقيقي عبر Kestrel وبروفايل Release؛ TestServer يبقى للاختبارات الوظيفية ولا يمثل شبكة أو TLS.
- [ ] فصل cold start للعملية وJIT عن أول connection وعن database wake-up، وفصل warm API cache وwarm DB buffers. لا نسمّي أول GET «cold DB» دون إثبات.
- [ ] datasets اصطناعية ثابتة: Small نحو 1K تشغيلية؛ Medium نحو 100K حركات؛ Large نحو 1M حركات مع سجلات audit الموافقة، وعدة مواقع ومستودعات ونطاقات وتوزيع غير متساوٍ.
- [ ] قياس p50/p95/p99 وthroughput وunexpected errors وtimeouts و429 وCPU/RSS/allocations/GC/thread-pool وSQL count/duration وpool/lock wait وحجم الاستجابة.
- [ ] warm-up منفصل ثم نافذة ثابتة 10–15 دقيقة مبدئيًا، وتكرار 3 runs؛ تسجيل عدد العينات وعدم نشر p99 كاستنتاج قوي من عينة صغيرة.
- [ ] سيناريوهات 1 و10 و25 و50 عميلًا أولًا، ثم تحديد نقطة التشبع دون وعد بتحمل رقم لم يُقَس. load driver بمعدل وصول معلوم لرصد تراكم الطلبات.
- [ ] فصل حمل طبيعي عن abuse tests. تحت الحمل الطبيعي المتوقع نراقب 429 ولا نخفيها كأخطاء «مقبولة» لتحسين الأرقام.
- [ ] مقارنة PostgreSQL محلي وNeon اختياريًا على قاعدة اختبار مستقلة وبنفس البيانات والتطبيق؛ تسجيل region/RTT/pool/warm-up. لا نستنتج أن Neon السبب من زمن الطلب وحده.
- [ ] قياس نسختين: workload بالـworkers والتدقيق الحقيقي، وعينة معزولة لتحديد الكلفة. إعداد Performance يعطل auditor حاليًا ويجب بيان ذلك في النتيجة.

المخرجات: `BASELINE-METRICS.json` و`WORKLOAD.md` وتقارير query counts وtraces منقحة. لا تستخدم IDs أو البريد أو raw SQL كـmetric labels عالية التنوع.

### أهداف أولية للتثبيت بعد baseline

الأرقام التالية ميزانيات مقترحة لبيئة اختبار معلنة، warm، وMedium dataset وحمل متفق عليه؛ ليست نتائج أو ضمانًا لNeon.

| العملية | p95 مبدئي | شرط إضافي |
|---|---:|---|
| قراءة سجل بسيط | 150ms | query count ثابت |
| قائمة 20 عنصرًا/بحث عادي | 250ms | لا تحميل كامل النتائج |
| تفاصيل مستند متوسط | 400ms | لا نمو استعلامات خطي مع السطور |
| تقرير عادي بفترة محدودة | 1000ms | سقف موارد وفلاتر واضحة |
| posting لمستند 100 سطر | 2000ms | ذرّية كاملة وaudit صحيح |
| Login | يحدد بعد profiling للـKDF | لا تخفيض قوة الحماية لتلبية هدف شكلي |

هدف الأخطاء غير المتوقعة أقل من 0.1% تحت الحمل الطبيعي المعلن، مع تقرير منفصل ل429 وtimeouts. أي تراجع مستمر >10% في مسار غير مستهدف يُحقق فيه على runner ثابت قبل القبول.

## المرحلة 5 — Login أسرع دون كسر hashes القديمة

- [ ] تقسيم الزمن إلى user lookup وKDF وtoken issuance وSaveChanges/audit والاتصال والشبكة. قياس missing-user/wrong-password/valid-password والحالة المعطلة.
- [ ] مراجعة فهرس البريد normalized وعدم التكرار، وتحميل الحد الأدنى اللازم فقط.
- [ ] إبقاء generic credential error وdummy hash المناسب لنوع الخوارزمية؛ لا cache لكلمة السر أو نتيجة التحقق.
- [ ] إذا أثبت القياس الحاجة: صيغة hash ذات version/algorithm/iterations/salt/hash، مع قراءة legacy صيغة SHA512/500000 كما هي وترقية عند login صحيح.
- [ ] مقارنة KDF المناسب وموازنته مع إرشادات OWASP المحدثة وحدود CPU/ذاكرة السيرفر؛ العدد القديم لا يتغير بمجرد تغيير constant. تأجيل MFA للمدير كمسار منتج مستقل إذا اختير، مع تقدير منفصل.
- [ ] حماية التحقق المكلف بحد concurrency مضبوط؛ لا Task.Run عشوائي حول hashing كحل للضغط.

بوابة الانتهاء: الحسابات القديمة تدخل، hash غير صالح لا يسبب 500، مقاومة brute force والتسريب الزمني محفوظة، وbefore/after يبين مصدر التحسن. التراجع يحتفظ بالقدرة على قراءة كل صيغة صدرت بالفعل.

## المرحلة 6 — القراءة والبحث والصفحات العميقة

- [x] لقوائم custody/counterpart: `UNION ALL` بإسقاط موحد ثم العد والترتيب والتقسيم داخل PostgreSQL؛ لا تُجلب `offset + pageSize` من كل جدول إلى الذاكرة.
- [ ] ترتيب كلي ثابت يشمل النوع والـID عند دمج جداول متعددة؛ اختبار تطابق totals وعدم ضياع أو تكرار الصفوف عند التعادل.
- [ ] cursor اختياري للجداول المتنامية عند الحاجة، مع بقاء page/pagination الحالية؛ لا حذف total_items أو تغيير response صامتًا. توثيق seek/filter contract وحدود cursor.
- [ ] اختيار top slow queries من القياس، مع `EXPLAIN (ANALYZE, BUFFERS)` على بيانات الاختبار. ANALYZE ينفذ SQL؛ لا نجرب أوامر كتابة بهذه الطريقة على بيانات فعلية.
- [ ] مراجعة فهارس trigram الموجودة واستفادة البحث العربي/Unicode وILIKE منها، والبحث القصير وشروط scope. لا إضافة GIN لكل عمود ولا حذف فهرس من idx_scan قصير المدة.
- [ ] تحسين تفاصيل المستند والتقارير بـprojections وbatch lookup وتخفيف round trips؛ لا عمليات EF متوازية على DbContext واحد.
- [ ] joins/EXISTS أو CTE للنطاقات الكبيرة عند قياس بطء arrays/Contains، مع اختبار hierarchy cycles وحدود العمق وسلامة النطاق.
- [ ] تقارير/exports كبيرة: حدود فترة وحجم، streaming أو job إذا أثبت القياس الحاجة؛ authorization عند الإنشاء والتنزيل وانتهاء صلاحية الملف.

بوابة الانتهاء: page عميقة لا تخصص ذاكرة متناسبة مع كل الصفحات السابقة، نتائج القديم والجديد متطابقة، query budgets ثابتة حيث يلزم، وتحسن مثبت للأبطأ دون تسرب cross-scope. فهارس جديدة مع migration وخطة تراجع مستقلة.

## المرحلة 7 — الكتابة والترحيل والتدقيق والمهام الخلفية

- [ ] قياس 1/100/1000 سطر مع اختلاف عدد مفاتيح المخزون، والتحقق من الميزانيات الواقعية للأصول المنشأة بدل قبول الحدود الافتراضية الكبيرة بلا قياس.
- [ ] قياس زمن الأقفال وترتيبها ومدة المعاملة، وتقليل العمل قبل/داخل المعاملة حيث لا يتعارض مع invariants. لا إزالة lock لتحقيق رقم أسرع.
- [ ] تقليل جولات SaveChanges فقط بعد تتبع اعتماد ledger totals وaudit/lifecycle عليها؛ الموجود set-based يُحافظ عليه.
- [ ] إن استُخدم bulk/set-based write: تعويض hooks وaudit والقيود صراحة مع اختبارات؛ ExecuteUpdate لا يشغل interceptors المعتادة تلقائيًا.
- [ ] اختبار تكرار Idempotency-Key بنفس/مختلف body وactor/scope، والتزامن وفقد الاستجابة بعد commit. retry للمعاملات الآمنة كوحدة، وليس لإجراء جزئي غير idempotent.
- [ ] PostgreSQL transient failures وdeadlock/cancellation لا تترك قيدًا جزئيًا أو تنشر side effects مرتين؛ inbox/outbox فقط للأحداث التي تحتاج الضمان.
- [ ] cleanup/auditor workers: batches محدودة وretry/backoff/idempotency وإيقاف منظم، وlease عند تعدد النسخ، وعدم ابتلاع الأخطاء دون metric.

بوابة الانتهاء: post/reverse متزامنان لا يكرران الأثر، balance يطابق ledger، التدقيق مكتمل، وموارد المستندات القصوى تحت الحد أو تُرفض مبكرًا. التراجع يحافظ على البيانات append-only.

## المرحلة 8 — PostgreSQL والاتصالات والتشغيل

- [ ] pg_stat_statements في بيئة القياس مع الصلاحية والإعداد اللازمين؛ استخدام `total_exec_time` و`mean_exec_time` الموافقة لإصدار السيرفر بدل نسخ أعمدة قديمة من skill.
- [ ] ميزانية connections تشمل عدد replicas وworkers والمهاجرات وهامش الإدارة؛ ضبط Npgsql pooling وtimeouts وفق القياس، وتحديد direct/pooled Neon لكل استخدام دون افتراض أن pooling يعالج network latency.
- [ ] statement/lock/request timeouts متناسقة، cancellation يصل لـSQL والملفات؛ اختبار pool exhaustion وإعادة الاتصال دون retry storm.
- [ ] مراجعة autovacuum/statistics/bloat/FK indexes وslow plans؛ لا tuning عامًا لـwork_mem دون حساب الاتصالات والعمليات المتزامنة.
- [ ] فصل migration owner عن runtime؛ runtime لا يملك تعطيل audit triggers أو DDL. اختبار التطبيق بكل صلاحياته على الدور المقيد.
- [ ] migrations طويلة بفحص lock/disk والنسخ الاحتياطي؛ concurrent indexes عند ملاءمتها خارج transaction block، والتحقق من invalid indexes بعد الفشل.
- [ ] backup/restore rehearsal وRPO/RTO متفق عليهما يشملان DB والمرفقات والاتساق بينهما. retention لسجلات audit/refresh وفق السياسة، لا حذف عشوائي لتحسين السرعة.
- [ ] partitioning/replicas/RLS خيارات مشروطة بالحجم والقياس، لا إضافات تلقائية. replica lag لا يصلح لفحص صلاحيات يجب أن يتوقف فورًا.

بوابة الانتهاء: دور runtime ينجح في الأعمال ويفشل في تعديل الحمايات، الحمل لا يستنزف pool، وتجربة الاستعادة موثقة. تغييرات Neon/الأسرار/إعدادات الخادم تحتاج صلاحية تشغيل محددة عند التنفيذ.

## المرحلة 9 — المرفقات وحدود كشف البيانات

- [ ] signatures الحالية تتحقق من مقدمة الملف ولا تثبت خلوه من malware؛ تحديد أنواع الملفات المدعومة واختبار truncated/polyglot/oversized files واستهلاك parser.
- [ ] حسب مصدر الرفع: quarantine/scanning قبل الإتاحة؛ إذا أُقر الفحص، تعطل scanner يبقي الملف محجوزًا بدل اعتباره سليمًا.
- [ ] تفويض upload/list/download/delete لكل documentId/attachmentId وتبديل المعرفات؛ التخزين خارج web root وأسماء server-generated.
- [ ] streaming وasync I/O ورفع محدود التزامن، ومنع تحميل ملفات كبيرة بالكامل في الذاكرة؛ filename آمن وContent-Disposition مناسب وnosniff.
- [ ] مراجعة JSON errors/logs/telemetry/SQL parameters وrequest IDs لمنع secrets وPII؛ logs الوصول إليها وretention جزء من الحماية.
- [ ] مراجعة compression للاستجابات الحاملة للأسرار وبيانات يعكسها العميل؛ استبعاد auth من الضغط إن انطبق الخطر بدل ضغط كل HTTPS JSON افتراضيًا.

بوابة الانتهاء: ملف خارج النطاق غير قابل للوصول، الفشل ينظف الأثر أو يعالجه worker بأمان، واختبار uploads المتزامنة لا يستنزف الذاكرة.

## المرحلة 10 — CI والمراقبة ومنع الرجوع

- [ ] CI للـPRs إلى main إضافة إلى push، دون تشغيل كود PR غير موثوق مع أسرار أو `pull_request_target` غير آمن.
- [ ] Actions مثبتة إلى SHAs متحقق منها، bot updates، minimal permissions، وسجل dependency/SBOM مناسب.
- [ ] تدقيق NuGet بصيغة JSON/version معروفة وفشل عند errors أو unavailable audit، لا تحليل جملة إنجليزية وحدها. secret scanner منقح وallowlist للاختبارات.
- [ ] fast build/unit/architecture/targeted security على PR؛ PostgreSQL integration وفق CI؛ load/soak/nightly على runner مستقر بمخرجات قابلة للمقارنة.
- [ ] اختبارات الأداء explicit تعمل بجدول واضح، والفشل يفشل المهمة. حفظ commit/seed/metrics بدل ملفات مؤقتة تتداخل بين runs.
- [ ] مراقبة 401/403/429 وreplay/bootstrap وadmin changes وinvalidation lag وSQL/lock/pool wait وp95/p99 وأخطاء workers؛ labels محدودة دون بيانات شخصية.
- [ ] probes بسيطة ومحدودة الكلفة؛ حماية diagnostics/metrics وعدم جعلها مصدر DoS أو كشف داخلي.

بوابة الانتهاء: PR يقدم تراجع صلاحيات أو query budget معروف يفشل في CI، والتنبيه مرتبط بإجراء تشغيلي واضح. ملفات docs الجديدة يجب تسليمها رغم أن `.gitignore` الحالي يتجاهل Markdown؛ اعتماد استثناءات محددة عند إدخالها للمستودع.

## المرحلة 11 — التحقق النهائي والتسليم

- [ ] إعادة مصفوفة الأمن: anonymous، viewer، editor، scoped warehouse/site/unit، administrator، suspended، revoked/expired tokens، وموارد خارج النطاق.
- [ ] اختبار حمل مختلط مع BOLA/IDOR وrefresh replay وتغييرات scope أثناء القراءة والكتابة، واختبار نسختين إن كان النشر متعدد النسخ.
- [ ] soak test مبدئي 60–120 دقيقة على dataset مناسب؛ تسجيل memory growth/pool exhaustion وrecovery بعد ضغط قصير. المدة النهائية ترتبط ببيئة الاستخدام.
- [ ] مقارنة قبل/بعد بنفس البيئة وdataset؛ كل تحسن موثق وكل تراجع مفسر. لا نسب تحسن متخيلة.
- [ ] staging مع TLS/proxy والاتصال الفعلي ودور runtime المقيد؛ smoke workflow أدمن من login حتى post/reverse/reports.
- [ ] مراجعة نهائية لفروق الكود والهجرات وعقد الفرونت والتشغيل، وإغلاق أو قبول موثق لكل ملاحظة.

المخرجات: `FINAL-REPORT.md` و`BEFORE-AFTER.md` و`OPERATIONS-RUNBOOK.md` و`FRONTEND-HANDOFF.md` و`SIGNOFF.md` داخل `security-performance-v2/`.

بوابة الإصدار: لا Critical/High مثبتة مفتوحة، مخاطر Medium لها قرار وصاحب إجراء، جميع الاختبارات المطلوبة نجحت، SLO المعلن تحقق، وrollback/restore عمليان. شهادة «بلا أي أخطاء» ليست مخرجًا قابلًا للإثبات.

## 5. قواعد التغيير والتراجع المشتركة

1. تغييرات DB إضافية متوافقة أولًا، ثم التطبيق، ثم cleanup بعد تأكيد الانتقال؛ لا إسقاط أعمدة أو إبطال hashes قديمة مبكرًا.
2. منع استخدام feature flag لتجاوز permission أو scope. flag للتحسين يعيد مسارًا صحيحًا سابقًا فقط.
3. لا output cache لاستجابات مستخدمين متعددين دون مفاتيح وعزل واختبار، ولا اعتبار cache hit دليل صحة.
4. لا تعطيل audit أو validation أو KDF أو locks في مقارنة السرعة الأساسية.
5. يبقى frontend handoff خطوة صريحة قبل أي تعديل يكسر عقده؛ لا نفترض أنه انتقل للـcookies.
6. تدوير أسرار Neon يتم خارجيًا عند الحاجة؛ لا نعيد كتابة Git history أو نغير صلاحيات قاعدة فعلية في مرحلة التخطيط.

## 6. حجم العمل التقديري

| الحزمة | تقدير أولي لأيام عمل تطوير ومراجعة |
|---|---:|
| الجرد وإثبات ومعالجة P0: المراحل 0–3 | 5–9 |
| القياس وLogin والقراءة والكتابة: 4–7 | 6–11 |
| تشغيل DB والملفات وCI: 8–10 | 4–7 |
| اختبارات staging وتسليم 11 | 2–4 |

المجموع التقديري 17–31 يوم عمل، لا وعد زمني. لا يشمل انتظار الفرونت أو صلاحيات الإنتاج أو MFA/منصة jobs جديدة إن اختيرت. يعاد التقدير بعد baseline؛ يمكن إصدار إصلاحات P0 مبكرًا بعد اجتياز اختبارات نطاقها.

## 7. مصادر رسمية ومرجع التحقق

- [Microsoft: تغير التعامل مع البروكسيات غير المعروفة](https://learn.microsoft.com/en-us/dotnet/core/compatibility/aspnet-core/8.0/forwarded-headers-unknown-proxies).
- [كود ForwardedHeadersMiddleware للإصدار 10.0.7](https://raw.githubusercontent.com/dotnet/aspnetcore/v10.0.7/src/Middleware/HttpOverrides/src/ForwardedHeadersMiddleware.cs): شرط `checkKnownIps` يفسر أولوية SP-01؛ نطابق إصدار runtime أثناء التنفيذ.
- [Microsoft: HybridCache](https://learn.microsoft.com/en-us/aspnet/core/performance/caching/hybrid?view=aspnetcore-10.0): نفحص سلوك الإصدار المثبت ولا نفترض اتساق نسخ متعددة من وجود الكاش.
- [OWASP: Password Storage](https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html): مرجع اختيار KDF وكلفته عند القياس والترحيل.
- [PostgreSQL: pg_stat_statements](https://www.postgresql.org/docs/current/pgstatstatements.html): مرجع إعداد القياس وأسماء الأعمدة حسب الإصدار.

## 8. أول دفعة تنفيذ عند اعتماد الخطة

تثبيت baseline وجرد المسارات، ثم اختبارات SP-01 وSP-04 وحدود الطلبات لإغلاق الثقة الخاطئة بالبروكسي وكلفة الصفحات العميقة، ثم إثبات وإصلاح SP-02 وSP-03. يلي ذلك تشغيل baseline أداء صالح قبل تعديل hashing أو SQL أو إضافة cache جديد.
